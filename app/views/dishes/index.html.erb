      <h3 class="text-center">
        ENSEMBLE DES PLATS DISPONIBLES
      </h3>
      <br><br>
<div class="container-fluid">
  <div class="row">
    <div class="col-md-12">
    <% @dishes.each do |dish| %>
      <div class="row">
        <div class="col-md-4">
          <div class="thumbnail">
            <%= image_tag @dish2.picture.url(:medium)%>
            <div class="caption text-center">
              <h3>
                <%= dish.name %>
              </h3>

              <p>
                <a class="btn btn-primary" href="/dishes/2">En savoir plus</a>
              </p>
            </div>
          </div>
        </div>
<% end %>
