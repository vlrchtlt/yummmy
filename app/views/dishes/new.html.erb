<div class="container">
  <div class="row">
    <div class="col-xs-12 col-sm-6 col-sm-offset-3">
      <h1>Proposer un plat</h1>
            <%= simple_form_for(@dish) do |f| %>
              <%= f.error_notification %>
              <%= f.input :name, label: 'Votre plat', placeholder: "Envoyez du rêve ! (max 40 caractères)" %>
              <%= f.input :description, label: 'Description', placeholder: "Soyez explicite, faites nous saliver ce texte " %>
              <%= f.fields_for @ingredient do |p| %>
                <% p.input :name, label: "Ingrédients", as: :collection_select, collection: Ingredient.all.map { |i| i.name }.uniq %>
              <% end %>

        <%= f.button :submit, label: "Ajouter votre plat", class: "btn btn-primary" %>


      <% end %>




    </div>
  </div>
</div>
